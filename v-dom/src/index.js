import React from "react";
import ReactDom from "react-dom";

const element = <h2>Understanding of the Virtual DOM</h2>;
//console.log(element);

ReactDom.render(element, document.getElementById("root"));
